function calculateNumbers(var1, var2) {
    // Perform some calculation here
    // For example, let's add var1 and var2
    let result = var1 + var2;
  
    // Return the result
    return result;
  }
  
  // Example usage:
  let resultOfCalculation = calculateNumbers(5, 10);
  console.log(resultOfCalculation); // Output: 15  